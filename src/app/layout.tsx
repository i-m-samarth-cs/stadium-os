import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "StadiumOS Copilot",
  description: "AI-enabled real-time stadium operations and fan-experience platform",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased dark`}
      suppressHydrationWarning
    >
      <body className="min-h-full flex flex-col sm:flex-row bg-background text-foreground" suppressHydrationWarning>
        {/* Sidebar will be injected in the page or wrapped here. We'll wrap children with Sidebar layout later. */}
        {children}
      </body>
    </html>
  );
}
